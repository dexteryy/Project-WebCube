{
  "private": true,
  "name": "webcube-projects",
  "description": "Continuously updated JS infrastructure for modern web dev",
  "scripts": {
    "precommit": "webcube-precommit",
    "update": "npm run clear:deps && yarn",
    "upgrade": "npm run clear:lock && npm run update",
    "clear:lock": "rm -rf yarn.lock package-lock.json",
    "clear:deps": "npm run clear:deps:root && npm run clear:deps:webcube && npm run clear:deps:nodecube && npm run clear:deps:eslint && npm run clear:deps:example",
    "clear:deps:root": "rm -rf ./node_modules",
    "clear:deps:webcube": "rm -rf ./webcube/node_modules",
    "clear:deps:nodecube": "rm -rf ./nodecube/node_modules",
    "clear:deps:eslint": "rm -rf ./eslint-config-webcube/node_modules",
    "clear:deps:example": "rm -rf ./*-example-*/node_modules",
    "lint": "webcube-lint",
    "lint:error": "webcube-lint-error"
  },
  "engines": {
    "node": ">=6"
  },
  "lint-staged": {
    "*.{js,jsx}": [
      "npm run lint:error --",
      "git add"
    ]
  },
  "config": {
    "webcube": {
      "monorepo": {
        "root": "./"
      }
    },
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "devDependencies": {
    "cz-conventional-changelog": "^2.0.0",
    "husky": "^0.14.3",
    "lerna": "^2.2.0",
    "lint-staged": "^4.2.2"
  },
  "workspaces": [
    "webcube/",
    "nodecube/",
    "eslint-config-webcube/",
    "*-example-*/"
  ]
}
